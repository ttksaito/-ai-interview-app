{
  "name": "ai-interview",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "install:all": "cd frontend && npm install && cd ../backend && npm install && cd ../api && npm install"
  },
  "workspaces": [
    "frontend",
    "backend",
    "api"
  ]
}
